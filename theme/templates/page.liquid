<section class="sec" data-reveal>
  <div class="wrap wrap--narrow">
    <div class="sec-head">
      <span class="sec-head__pre">helbrede</span>
      <h1 class="sec-head__main">{{ page.title }}</h1>
    </div>
    <div class="prose">{{ page.content }}</div>
  </div>
</section>
